# JAX with CUDA support (for GPU)
jax[cuda12]>=0.4.20
jaxlib>=0.4.20

# Flax NNX for neural networks
flax>=0.8.0

# Tokenizer and model config
transformers>=4.51.0

# Weight file loading
safetensors>=0.4.0

# Fast hashing for prefix cache
xxhash>=3.0.0

# NumPy for CPU-side operations
numpy>=1.24.0
